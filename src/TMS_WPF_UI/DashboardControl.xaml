<UserControl x:Class="TMS_WPF_UI.DashboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="520"
             d:DesignWidth="760"
             Loaded="UserControl_Loaded">
    <Grid Background="#F5F7FA" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="Treasury Positions"
                       FontSize="26"
                       FontWeight="SemiBold"
                       Foreground="#172033"/>
            <TextBlock Text="Currency-level exposure calculated from captured trades"
                       Foreground="#5D6678"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <DockPanel Grid.Row="1" Margin="0,0,0,12">
            <Button DockPanel.Dock="Left"
                    Content="Refresh"
                    Width="96"
                    Height="32"
                    Command="{Binding RefreshPositionsCommand}"/>

            <Button DockPanel.Dock="Left"
                    Content="New Trade"
                    Width="100"
                    Height="32"
                    Margin="8,0,0,0"
                    Click="NewTrade_Click"/>

            <Button DockPanel.Dock="Left"
                    Content="Telerik View"
                    Width="112"
                    Height="32"
                    Margin="8,0,0,0"
                    Click="TelerikDashboard_Click"/>

            <TextBlock Text="{Binding StatusMessage}"
                       VerticalAlignment="Center"
                       Foreground="#5D6678"
                       Margin="12,0,0,0"/>
        </DockPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Positions}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  Background="White"
                  BorderBrush="#D9DEE8"
                  BorderThickness="1"
                  RowHeight="34"
                  AlternatingRowBackground="#F9FAFC">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Currency" Binding="{Binding Currency}" Width="110"/>
                <DataGridTextColumn Header="Buy Notional" Binding="{Binding BuyNotional, StringFormat=N2}" Width="150"/>
                <DataGridTextColumn Header="Sell Notional" Binding="{Binding SellNotional, StringFormat=N2}" Width="150"/>
                <DataGridTextColumn Header="Net Notional" Binding="{Binding NetNotional, StringFormat=N2}" Width="150"/>
                <DataGridTextColumn Header="Avg Rate" Binding="{Binding WeightedAverageRate, StringFormat=N6}" Width="130"/>
                <DataGridTextColumn Header="Trades" Binding="{Binding TradeCount}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3"
                   Text="Hosted path: WinForms shell -> ElementHost -> WPF UserControl -> MVVM ViewModel -> CoreAPI"
                   Foreground="#7A8496"
                   Margin="0,12,0,0"/>
    </Grid>
</UserControl>
